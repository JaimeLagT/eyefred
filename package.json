{
  "dependencies": {
    "@nut-tree-fork/nut-js": "^4.2.6",
    "@radix-ui/react-dropdown-menu": "^2.1.15",
    "electron-store": "^10.0.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-icons": "^5.5.0"
  },
  "scripts": {
    "dev": "concurrently -k -r \"npm run dev:vite\" \"npm run dev:electron\"",
    "dev:vite": "cd frontend/renderer && npm run dev",
    "dev:electron": "wait-on http://localhost:5173 && npx electron frontend/main.js",
    "build": "vite build && electron-builder"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "electron": "^36.3.1",
    "electron-builder": "^26.0.12",
    "wait-on": "^8.0.3"
  }
}
