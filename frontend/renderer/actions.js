module.exports = {
    performAction: (actionName) => {
        console.log(`(stub) performAction called with: ${actionName}`);
        // Later: add real logic to launch apps, send media keys, etc.
    }
};
